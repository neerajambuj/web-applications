
Please make sure to install virtual envirionment using the below command
virtualenv -p /usr/bin/python3 venv

After installing , activate virtual environment using below commmand
source venv/bin/activate

After that install requirement.txt which contails all the packages that are requuired to run the bot
using this command : pip install -r requirement.txt
